#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
    char *str;
    char *str2;

    str = "I do not fear computers. I fear the lack of them - Isaac Asimov";
    str2 = "My school";
    print_rev(str);
    print_rev(str2);
    return (0);
}
